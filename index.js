/**
 * @format
 * @lint-ignore-every XPLATJSCOPYRIGHT1
 */

import {AppRegistry} from 'react-native';
import App from './src/App';
import {name as xcape} from './app.json';

AppRegistry.registerComponent(xcape, () => App);
